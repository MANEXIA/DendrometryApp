<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Theme.AppCompat.Light.NoActionBar / use this to change color button -->
    <!--    DEFAULT: Theme.Material3.DayNight.NoActionBar -->
    <style name="Base.Theme.MyAppKotlin" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyAppKotlin" parent="Base.Theme.MyAppKotlin" />

    <style name="RoundedMaterialDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="shapeAppearanceOverlay">@style/CustomDialog</item>
        <item name="colorSurface">@color/white</item>

    </style>

    <style name="CustomDialog" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

</resources>